(function(){'use strict';const root=document.documentElement,theme=document.getElementById('theme'),media=matchMedia('(prefers-color-scheme: light)');function apply(){root.dataset.themeChoice=theme.value;root.dataset.theme=theme.value==='system'?(media.matches?'light':'dark'):theme.value;}theme.value=root.dataset.themeChoice||'dark';theme.addEventListener('change',()=>{apply();try{localStorage.setItem('om-theme',theme.value);}catch(e){}});media.addEventListener('change',apply);const menu=document.querySelector('.menu-toggle'),nav=document.getElementById('navigation');function closeMenu(){menu.setAttribute('aria-expanded','false');nav.classList.remove('open');}menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open);});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu.getAttribute('aria-expanded')==='true'){closeMenu();menu.focus();}});document.querySelectorAll('[data-year]').forEach(el=>el.textContent=String(new Date().getFullYear()));const dialog=document.getElementById('image-dialog');if(!dialog||!dialog.showModal)return;let opener;document.querySelectorAll('[data-lightbox]').forEach(link=>link.addEventListener('click',e=>{e.preventDefault();opener=link;const img=link.querySelector('img');dialog.querySelector('img').src=link.href;dialog.querySelector('img').alt=img.alt;dialog.querySelector('.dialog-caption').textContent=img.alt;dialog.showModal();}));dialog.querySelector('button').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});dialog.addEventListener('close',()=>opener?.focus());})();
